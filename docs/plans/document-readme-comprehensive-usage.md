# README Scope

The README intentionally contains only the operational essentials:

- What `ObliqueStrategies` does
- Frozen uv installation against the pinned MCP SDK
- The three output modes
- Claude Desktop configuration
- The protocol test command

Avoid restoring redundant run methods, sample-output galleries, project tours, or
generic troubleshooting sections unless the project gains a concrete need for them.
