[project]
name = "oblique-thinking-strategies-mcp"
version = "0.1.0"
description = "An MCP server for randomized Oblique Strategies prompts"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]==2.0.0",
]
